List the background tasks of the current session.

- `active_only` defaults to true; set it false to include finished and failed tasks.
- Use it to recover task IDs when you are no longer sure which are still running, such as after compaction.
- Read-only, and safe to use in plan mode.
