Replace text in a file. Supports exact, fuzzy (whitespace-tolerant), and regex matching. Multi-line edits supported.

Set `regex: true` to treat `old` as a regex. Use `replace_all: true` to replace all occurrences.
