Write content to a file, creating it if it does not exist.

- The default `overwrite` mode replaces the whole file; `append` adds to the end of it.
- To change part of a file, use StrReplaceFile rather than rewriting it.
