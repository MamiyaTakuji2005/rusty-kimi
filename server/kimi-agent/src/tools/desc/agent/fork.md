Fork this agent into a background copy that shares the conversation so far.

- The fork receives your `prompt` as its next user message and runs while you continue.
- Use it for a tangent or a parallel sub-task where the shared history matters; use Agent when it does not.
- Returns a task ID — collect the result with TaskOutput.
