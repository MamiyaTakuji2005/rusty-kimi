<?xml version="1.0" encoding="utf-8"?>
<!-- The mark: "2 squared" as a 2x2 grid — the arithmetic the project is named
     after — on the dark teal tile. Full-bleed 108dp background for the
     adaptive icon (content belongs in the centered ~66dp safe zone, which is
     the foreground's job). -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <path
        android:fillColor="#0E2A32"
        android:pathData="M0,0h108v108h-108z" />
</vector>
