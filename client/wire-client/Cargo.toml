[package]
name = "wire-client"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
chrono.workspace = true
serde.workspace = true
serde_json.workspace = true
toml.workspace = true
tokio.workspace = true
kaos = { path = "../../server/kaos" }
dvadva-agent = { path = "../../server/dvadva-agent" }
kosong = { path = "../../server/kosong" }

[dev-dependencies]
# Only for the drift-guard test: the client- and daemon-side bridge framings
# live in separate crates by design; the test pins them byte-for-byte.
dvadva-bridge = { path = "../../remote/dvadva-bridge" }
